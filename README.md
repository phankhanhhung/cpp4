This demonstrated the use of GoogleTest in this project. The test cases are in the test folder. The test cases are run by the following command:
```
cmake -S . -B build
cmake --build build
cd build && ctest  
```
